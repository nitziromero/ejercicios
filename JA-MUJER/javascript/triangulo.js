// Paso 1: Definir las variables para la base y la altura del triángulo.
var base = 5; // Puedes cambiar este valor por la longitud de la base del triángulo.
var altura = 8; // Puedes cambiar este valor por la altura del triángulo.

// Paso 2: Calcular el área del triángulo utilizando la fórmula: Área = (base * altura) / 2.
var area = (base * altura) / 2;

// Paso 3: Imprimir el resultado en la consola.
console.log("El área del triángulo con base " + base + " y altura " + altura + " es: " + area);
